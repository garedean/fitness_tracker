





<div class="container"
  <div class="row user">
    <div class="col-sm-5">
      <h1><%=@user.name%>'s Fit Journal</h1>
    </div>
  </div>
</div>

<div class="container">
  <div class="row">
    <div class="col-sm-8 col-sm-offset-2">
      <%= bootstrap_form_for [@user, @new_food] do |f| %>
        <%= f.text_field :calories %>
        <%= f.submit %>
      <% end %>

    </div>
  </div>
</div>
